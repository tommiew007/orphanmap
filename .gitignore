# Python
__pycache__/
*.pyc

# Generated output (raw captures may contain machine/user identifiers)
output/

# Local environment setup, install logs, and the live fixture (contain absolute
# paths, machine name, and account names for THIS box). Sanitize before publishing.
setup/
fixture_files/

# GUI/tool state
*.state.json

# Launch drafts / internal notes — keep in the working tree, never publish
docs/

# Marketing landing page (orphanmap.com) + email-capture function — deployed
# separately to its own Cloudflare Pages project, kept out of the OSS repo
site/
